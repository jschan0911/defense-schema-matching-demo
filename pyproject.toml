[project]
name = "defense-schema-matching-demo"
version = "0.1.0"
description = "Synthetic C2 screenshot-guided SCHEMORA demonstration"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[tool.pytest.ini_options]
testpaths = ["tests"]
